name: Daily Bolao Copa 2026 Email (disabled)

# This automation has been intentionally disabled to stop all routine activity.
# The scheduled daily email trigger was removed.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled notification
        run: echo "The bolao-copa-2026 automation has been disabled and no routine jobs remain active."
